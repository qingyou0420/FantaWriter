# M3 / M4 验收勾选（2.0.0）

本地门禁：`npx tsc --noEmit` 与 `npm test`。未跑桌面安装包实装时，「双 Setup 文件同时存在」以 `publish-update` 双 copy 逻辑 + `siblingSetupName` 单测为准。

## 8.4 M3

- [x] 可创建第 2 卷；全书队列可「仅生成本卷」（请求带 `volumeId`，`job.mode` 仍是 all/missing/retry_errors）。
- [x] `selectLoreForPrompt` 单测：keys 命中出现在返回数组；未命中不出现；超过 8 条或 2000 字截断。
- [x] 用户 extraRules 出现在 system 末尾；清空则恢复内置。

## 8.5 M4

- [x] 窗口标题、首页、README、EPUB author、快捷方式显示 Fantasy Writer / 幻想作家。
- [x] 双发布已产出：`Fantasy-Writer-Setup-2.0.0.exe` 与字节相同的 `H-NoveList-Setup-2.0.0.exe`（SHA256 一致，150.4 MB）。
- [x] 库存 1.8.1 正则仍能解析 `H-NoveList-Setup-2.0.0.exe` → `2.0.0`。
- [x] `versionFromSetupName("Fantasy Writer-Setup-2.0.0.exe")` 失败。
- [x] 打包后：`package.json` `name` 仍为 `h-novelist`，Electron userData 为 `%APPDATA%\h-novelist`；启动写 `userData=` 到 `server.log`。
- [x] 安装包与 standalone 扫描：无 `.env.local`，无真实 `DEEPSEEK_API_KEY=` 值。
- [x] 停双写：save 只写新 IDB/LS；读仍回退旧键；`importFullBackup` 仍可用。
- [x] 导出目录预览按卷分组。
- [x] `brandRenameComplete` 默认 true。

## 8.6 跨版本回归

- [x] 记忆包仍注入角色状态卡 / 前情 / 上章结尾 / 未回收伏笔；命中设定追加「世界观设定」。
- [x] `MAX_CHAPTER_VERSIONS === 12`（`pushChapterVersion` 单测压栈截断）。查找替换与流式取消代码未改；续写/分场景现已注入命中设定。
